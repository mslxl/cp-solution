#include<bits/stdc++.h>
#ifdef Local
#define test if(1)
#else
#define test if(0)
#endif
#define rep(i,s,e) for(int i = (s); (i) < e; i++)
typedef long long ll;
struct Person{
	int id;
	int house_num;
	int house_cover;
	int next;
} nodes[1001];
int head[10000], eid = 0;
void add_edge(int u, int v){
	eid++;
	nodes[eid]
}
int main(){
	std::ios::sync_with_stdio(false);
	std::cin.tie(nullptr);
	std::cout.tie(nullptr);
	int N;
	rep(i, 0, N){
		n
	}
	return 0;
}

