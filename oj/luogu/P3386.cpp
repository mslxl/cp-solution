#include<bits/stdc++.h>

int n,m,e;
int main(){
	std::ios::sync_with_stdio(false);
	std::cin.tie(nullptr);
	std::cout.tie(nullptr);
	std::cin >> n >> m >> e;
	for(int i = 0 ; i < e; i++){
		int u,v;
		std::cin >> u >> v;
	}
}}
