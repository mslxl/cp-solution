#include <bits/stdc++.h>
#ifdef Lo
#define test if (1)
#else
#define test if (0)
#endif

void run() {}

int main() {
  std::ios::sync_with_stdio(false);
  std::cin.tie(nullptr);
  std::cout.tie(nullptr);
  int T;
  std::cin >> T;
  while (T--)
    run();

  return 0;
}
